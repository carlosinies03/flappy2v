function preload() {
    // Load bird sprite with new dimensions
    this.load.spritesheet('bird', 'path/to/bird_sprite.png', {
        frameWidth: 120,
        frameHeight: 118
    });
    // Other preload assets...
}